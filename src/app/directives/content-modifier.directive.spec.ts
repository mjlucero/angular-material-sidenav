import { ContentModifierDirective } from './content-modifier.directive';

describe('ContentModifierDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentModifierDirective();
    expect(directive).toBeTruthy();
  });
});
